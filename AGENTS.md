# FocusWave Design System — AI Instructions

Read root `AI_PROJECT.md` first. It defines this repository's design role, central governance pointer, workspace registry and related-repository boundaries without duplicating policy.

Cross-AI governance is owned by `greenboo26/ai-governance@main`. If project/repository identity is uncertain, use `greenboo26/project@august/PROJECT_INDEX.md`.

For design work:

1. read `AI_PROJECT.md` and `README.md`;
2. inspect existing `system/`, `prototype/`, `content/`, `docs/` and archive evidence before inventing replacements;
3. use this repository as the design-system/prototype truth;
4. when the task requires real application implementation, verify and modify `kyandi233-dev/FocusWave` rather than claiming a prototype change is deployed;
5. when the task involves scientific meaning or measurement validity, resolve that evidence from `greenboo26/focuswave-multimodal-attention-analysis`;
6. verify actual rendered/deployed results before claiming completion.

Preserve unrelated work and history. Do not force-push or rewrite history without explicit authorization. Do not store credentials or private runtime/account state here.
